-- New equipment (MAJOR GAMEPLAY/WORLD INTEGRATION UPDATE): a strong
-- late-game single-target funnel that pulls a large amount out of one area
-- of the pile at once. Fixed-stat like FeatherGrabberConfig -- see its
-- header for why this isn't a 5-level upgrade track.

local TornadoLauncherConfig = {}

TornadoLauncherConfig.UNLOCK_COST_CENTS = 7500 -- $75.00
TornadoLauncherConfig.ONE_ROUND_ROBUX = 64
TornadoLauncherConfig.PERMANENT_ROBUX = 224

TornadoLauncherConfig.MAX_COLLECT_AMOUNT = 150
TornadoLauncherConfig.COOLDOWN_SECONDS = 6
TornadoLauncherConfig.SEARCH_RADIUS_COLUMNS = 5

return TornadoLauncherConfig
